apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: browser-platform-ingress
  namespace: browser-platform
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
    nginx.ingress.kubernetes.io/upstream-keepalive-timeout: "600"
spec:
  ingressClassName: nginx
  rules:
  - host: browser-platform.local  # Change this to your domain
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: browser-platform-service
            port:
              number: 80
  # Uncomment and configure for HTTPS
  # tls:
  # - hosts:
  #   - browser-platform.local
  #   secretName: browser-platform-tls
